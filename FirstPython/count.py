#!/usr/bin/python

import math;

def countdown(n):
    y = 0;
    while x > 0:
        x = x - 5;
        y = y + 1;
    print y;

def time(n):
    steps = math.ceil(n / 5.0) * 2 + 3;
    return steps;

print countdown(50);
